"use client";import{useEffect,useState}from"react";import{Shell}from"./Shell";export function EmployeesClient({name}:{name:string}){const[u,setU]=useState<any[]>([]);useEffect(()=>{fetch("/api/users").then(x=>x.json()).then(setU)},[]);return <Shell name={name}><h1>الموظفون</h1><p className="muted">ظهور الموظف هنا لا يمنح صلاحية الوصول إلى ملفاته أو مساحات عمله.</p><div className="card" style={{overflowX:"auto"}}><table className="table"><thead><tr><th>الموظف</th><th>البريد الإلكتروني</th><th>الحالة</th></tr></thead><tbody>{u.map(x=><tr key={x.id}><td>{x.avatarUrl?"🖼️ ":"👤 "}{x.name}</td><td>{x.email}</td><td><span style={{color:x.online?"#14804a":"#748b86"}}>● {x.online?"متصل":"غير متصل"}</span></td></tr>)}</tbody></table></div></Shell>}
