"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export function AuthForm({mode}:{mode:"login"|"register"}){const[error,setError]=useState("");const[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const form=new FormData(e.currentTarget);const r=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});setLoading(false);if(!r.ok){setError((await r.json()).error||"تعذر إكمال العملية");return}router.push("/dashboard");router.refresh()}return <form onSubmit={submit} className="card" style={{padding:28,width:"min(420px,100%)",display:"grid",gap:14}}><h1 style={{margin:0}}>{mode==="login"?"تسجيل الدخول":"إنشاء حساب"}</h1>{mode==="register"&&<input className="field" name="name" placeholder="الاسم الكامل" required minLength={2}/>}<input className="field" name="email" placeholder="البريد الإلكتروني" type="email" required/><input className="field" name="password" placeholder="كلمة المرور (8 أحرف على الأقل)" type="password" required minLength={8}/>{error&&<p style={{color:"#b42318",margin:0}}>{error}</p>}<button className="btn" disabled={loading}>{loading?"جارٍ المعالجة...":mode==="login"?"دخول":"إنشاء الحساب"}</button><a className="muted" href={mode==="login"?"/register":"/login"}>{mode==="login"?"ليس لديك حساب؟ أنشئ حسابًا":"لديك حساب؟ سجل الدخول"}</a></form>}
