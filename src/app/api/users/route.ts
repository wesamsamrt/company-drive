import { NextRequest, NextResponse } from "next/server"; import { requestUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma";
export async function GET(request:NextRequest){if(!await requestUser(request))return NextResponse.json({error:"غير مصرح"},{status:401});const users=await prisma.user.findMany({select:{id:true,name:true,email:true,avatarUrl:true,lastSeenAt:true},orderBy:{name:"asc"}});return NextResponse.json(users.map((u:{id:string;name:string;email:string;avatarUrl:string|null;lastSeenAt:Date})=>({...u,online:u.lastSeenAt>new Date(Date.now()-5*60*1000)})));}
