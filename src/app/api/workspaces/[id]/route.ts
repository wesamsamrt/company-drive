import {NextRequest,NextResponse}from"next/server";import{prisma}from"@/lib/prisma";import{authorize}from"@/lib/access";
export async function GET(r:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;const a=await authorize(r,id,"view");if("error"in a)return NextResponse.json(a,{status:a.status});const workspace=await prisma.workspace.findUnique({where:{id},include:{members:{include:{user:{select:{id:true,name:true,email:true,avatarUrl:true,lastSeenAt:true}}},orderBy:{joinedAt:"asc"}},_count:{select:{files:true,folders:true}}}});return NextResponse.json({...workspace,myRole:a.member.role});}
export async function DELETE(r:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;const a=await authorize(r,id,"deleteWorkspace");if("error"in a)return NextResponse.json(a,{status:a.status});await prisma.workspace.delete({where:{id}});return NextResponse.json({ok:true});}
