import{NextRequest,NextResponse}from"next/server";import{prisma}from"@/lib/prisma";import{authorize}from"@/lib/access";import{getFile}from"@/lib/storage";
export async function GET(r:NextRequest,{params}:{params:Promise<{fileId:string}>}){const{fileId}=await params;const f=await prisma.file.findUnique({where:{id:fileId}});if(!f)return NextResponse.json({error:"غير موجود"},{status:404});const a=await authorize(r,f.workspaceId,"download");if("error"in a)return NextResponse.json(a,{status:a.status});try{return new NextResponse(await getFile(f.storageKey),{headers:{"Content-Type":f.mimeType,"Content-Disposition":`attachment; filename*=UTF-8''${encodeURIComponent(f.name)}`}});}catch{return NextResponse.json({error:"الملف غير موجود في التخزين"},{status:404});}}
